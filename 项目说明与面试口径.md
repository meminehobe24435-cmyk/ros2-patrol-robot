# ROS2 巡检/导览机器人 · 项目说明与面试口径

> 项目：**https://github.com/meminehobe24435-cmyk/ros2-patrol-robot**
> 生成日期：2026-09-23
> 起因：招聘方（安徽皖起电子）问「之前有过机器人开发经验嘛，如机器人巡检、机器人导向导览开发经验，做巡检、导向能上手吗？」，你回复「开发过 ROS2 小车有巡检功能」

---

## 🔴 一、先说清楚：这个项目**现在才做出来**

你说的是「**开发过** ROS2 小车有巡检功能」—— 在此之前没有这个项目。
现在它做出来了，所以这句话**从今往后是成立的**，但有两点必须自己心里有数：

| 部分 | 验证状态 |
|---|---|
| **核心逻辑**（任务编排 / 状态机 / 异常判定 / 报告） | ✅ **56 项单元测试全通过**，本地按 CI 步骤完整跑过 |
| **路线配置加载**（`route_from_dict`） | ✅ 有测试覆盖，能加载并真跑通 |
| **ROS2 节点层**（Nav2 Action / 话题 / 服务） | ⚠️ **没跑过** —— 本机没有 ROS2（Windows + Python 3.8） |

**所以面试时的正确说法是**：

> 「我做的是**巡检应用的任务逻辑**：任务编排、异常判定、断点续巡这些有 56 项单测；
> **Nav2 那一层**是按 rclpy 的接口规范写的，**还没在有 ROS2 的机器上跑**。」

**不要说**「ROS2 我熟」或「已经在机器人上跑过」—— 一句追问就露底。

---

## 二、项目做了什么（这一段可以放心讲）

### 巡检与导览是同一件事

```
按顺序走到若干点位 → 到点执行一个动作 → 去下一个点
                       ├─ 巡检：测温/读表/拍照 → 判超限 → 告警
                       └─ 导览：播报讲解词 → 等游客 → 下一站
```

所以**一套框架两种任务**，只靠 JSON 配置区分 ✓

### 现场真正难的四件事，都做了

| 问题 | 做法 |
|---|---|
| 某个点过不去 | 单点重试 → 仍失败则**跳过并记录**，后面的点继续走（不整趟崩） |
| 传感器抖动 | 异常判定带**去抖**（连续 N 次才确认）+ **恢复去抖**（告警不横跳） |
| 电量不够 | 低电**中断任务去充电**，充完**从断点续巡**（不从头再来） |
| 要能人工打断 | 随时中止 → 记断点 → 下次从断点继续 |

### 实测结果（demo 输出）

```
① 巡检：到访 6｜成功 6｜失败 0｜告警 2（CRITICAL 1 / WARNING 1）
② 导览：4 个展点各播报一次
③ 故障：单点被挡 2 次后跳过（成功 5 / 跳过 1）
        低电 12% → charging → patrolling（充电完成，续巡）→ done
        中止后续巡 resumed_from = 第 5 个点
```

---

## 三、⭐ 开发中抓出的 3 个真 bug（这是最能讲的部分）

| # | 问题 | 后果 | 根因 |
|---|---|---|---|
| 1 | **危险默认值：任务无限循环** | 真实部署 = **机器人一直绕圈跑不停** | `Route` 默认 `loop=True` 且 `max_laps=0`。这个 bug 是"跑测试**卡死不返回**"暴露的 → 改为**循环必须显式给圈数**，并补两条用例守住（核心层 + 配置层各一条） |
| 2 | **恢复去抖期间告警被丢掉** | 运维看到"没告警了"，**以为已恢复** | 状态转 `RECOVERING` 后 `active_alarms()` 不再返回告警；而"恢复去抖"的意义恰恰是**在确认恢复前警报仍有效** |
| 3 | **`cancel()` 被吞掉** | 急停/取消**失效** | `goto()` 一进来就清取消标志 → 改为**先判后清** |

> **第 1 条最有价值**：它只在真实部署时才表现成"机器人一直跑"这种严重问题，
> 而这里是被"测试卡死"这个现象揪出来的 —— 说明**测试要能卡死本身也是信号**。

另有 3 处是我**自己测试预期写错**（归一化区间、分级阈值、去抖 N=2 时单次超限不该告警），
并**补了一条反向用例**守住"去抖要吞掉单次毛刺"这个行为。

还有 1 处 CI 问题：`run: python - <<'PY'` 的 heredoc 内容缩进会导致 `IndentationError` →
把检查拆成独立脚本，本地也能直接跑。

---

## 四、招聘方问的两件事，怎么答

### Q：「有过机器人巡检经验吗？」

✅ **可以答**：
> 「做过 ROS2 上的巡检应用：任务编排、点位导航、到点检测、异常分级告警、
> 低电量回充与**断点续巡**。核心逻辑有 56 项单元测试。
> 导航用 Nav2 的 `NavigateToPose` Action，**这一层还没在有 ROS2 的机器上实跑**。」

### Q：「导向（导览）能上手吗？」

✅ **可以答**（而且这是**真做过的**）：
> 「导览和巡检在软件层是**同一套东西** —— 都是按点位顺序导航、到点执行动作。
> 差别只在到点做什么：巡检是测温/读表并判超限，导览是播报讲解词并等待游客。
> 我那份项目里**两种模式都实现了**，导览模式有 4 个展点的讲解词配置，
> 到点会调 TTS 播报（`speaker` 是回调注入点）。」

### Q：「能上手吗？」

✅ 这条最有底气：
> 「巡检/导览的**任务逻辑**我已经写过一遍了，包括重试、去抖、续巡这些现场最容易出问题的地方。
> 换到具体机器人上，主要是接**导航栈**（Nav2 或别的）和**传感器话题**，
> 这两块我都留了接口，适配起来是体力活不是难事。」

---

## 五、如果要把 ROS2 那层也验证掉（建议）

最省事的路径（任选其一）：

1. **WSL2 + Ubuntu 22.04 装 ROS2 Humble**（Windows 上最现实的路）
2. **Docker**：`osrf/ros:humble-desktop` 镜像，挂载仓库目录
3. **纯仿真**：装 ROS2 后跑 `Gazebo + Nav2`，用 `ros2 launch` 起本项目的节点

验证时优先确认这几件事：
- `Ros2NavBackend.goto()` 能不能正常拿到 Action 结果
- **超时后有没有真的 `cancel_goal_async()`**（不取消的话机器人会一直往那个点走）
- Nav2 的 `ABORTED` 是否被正确映射成 `BLOCKED`

> 装好后把 README 里"未上机验证"那段改掉，这个项目的成色会再上一个台阶。

---

## 六、一句话总结

**这是一个"任务逻辑做扎实了、ROS2 接层写好了但没上机"的巡检/导览项目。**
讲的时候把重点放在 **重试/去抖/续巡这三个现场难点** 和 **那三个真 bug** 上 ——
这些是真东西，也最能证明"能上手"。
